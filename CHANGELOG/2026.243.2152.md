# 2026.243.2152 — docs(skills): tighten the skill descriptions to trigger-style

## Summary

Collapse the `pod-skill` description in `charly.yml` from a multi-line block to a single line. The description content is unchanged (it was already trigger-style: "Schema reference for `kind: pod` and deploy entities: charly.yml entry shape, tree-position nesting, sidecars, and pod networking. Use the deploy skill for verb-level operations.") — only the YAML formatting is normalized. The description is the skill index line injected into every harness system prompt; the single-line form is the canonical shape. No capability reduction: the skill remains discoverable and loadable.

## How tested

- `python3` YAML parse of the edited `charly.yml`: OK — the `pod-skill` entity parses, description verified.
- `git diff` review: one `description:` block changed (formatting only); no structural key touched; no body content changed.
- The change is metadata-only prose; no behavior, schema, or build surface affected.

## Rulebook compliance

- **R3** — no duplication: the description is a single canonical index line.
- **R5** — no stale references: the description content is unchanged.
- No pins, no scripts, no schema changes, no direct push to main.

## Change classification

- **Change class:** metadata-prose (one skill `description:` field in `charly.yml`; zero behavior change)
- **Verification gate:** YAML parse + diff review
- **Attribution tier:** `analysed on a live system` — the changed path (the skill-description metadata) was parsed and verified live with retained output; the full R10 gate does not apply to a metadata-prose change with no runtime surface.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*